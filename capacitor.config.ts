import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'modus-create-poc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
